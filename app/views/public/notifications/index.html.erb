<% notifications = @notifications.where.not(visitor_id: current_member.id) %>
<% if notifications.exists? %>
  <%= render notifications %>
  <%= paginate notifications %>
<% else %>
<div class="col-md-6 mx-auto"><br><p class="h4">通知はありません</p></div>
<% end %>